# Start shell as login shell
shell -$SHELL

# Disable startup message
startup_message off

# Status line at the bottom
caption always
caption string "%{.bW}%-w%{.rW}%f%n %t%{-}%+w %=%{..W}[%H %l] %{..W} %D %d/%m/%Y %c "

# Alternate screen support
altscreen on
